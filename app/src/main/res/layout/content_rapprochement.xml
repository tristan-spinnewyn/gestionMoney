<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".RapprochementActivity"
    tools:showIn="@layout/activity_rapprochement"
    android:orientation="vertical"
    android:background="@color/colorPrimaryDark">
    <TableLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:stretchColumns="*"
        android:shrinkColumns="*"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"
        >
        <TableRow>
            <TextView
                android:id="@+id/debAcc"
                android:text="@string/actSolde"
                android:textColor="@color/white"
                android:gravity="center"
                />
            <TextView android:id="@+id/soldeAcc"
                android:text="@string/soldeAtt"
                android:textColor="@color/white"
                android:gravity="center"/>
            <Button
                android:text="Valider"
                android:id="@+id/valRapp"/>
        </TableRow>
    </TableLayout>
    <androidx.recyclerview.widget.RecyclerView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/lstOpRapp">

    </androidx.recyclerview.widget.RecyclerView>
</LinearLayout>